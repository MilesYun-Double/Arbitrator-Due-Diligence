# Issue #13 actual synthetic baseline evidence

These are byte-for-byte saved artifacts from the local fresh-process synthetic chain, not hand-authored Evidence or a production report. The three source folders preserve the collectors' actual outputs and snapshots. evidence.json is their exact merged array; report.json is the model passed to all formats. verification.json records copied file hashes. Absolute local source/snapshot paths are historical provenance for the measured run; do not treat this copied example directory as a live run root. Reproduce with the package/synthetic smoke to create a new valid run with its own paths. No real people or cases are included.

local-run.json records the actual local process (including file-backed import origins and stage times). package-run.json records the final baseline ZIP, fresh relocated full-chain PASS, and the separate no-Pillow experimental ZIP/failure trace. The no-Pillow failure is expected and confirms the retained platform dependency; it is not a passing dependency-free package. Original test/measurement logs and ZIP/runtime directories remain task-local, not packaged or duplicated into Git.

Evidence count 3; input HTML 241 bytes, text 66 bytes, digital PDF 2260 bytes / 3 pages. Local report: 6 pages / 70416 bytes. Font unchanged: 25575676 bytes. PDF dependency archives: 8978619 bytes; existing pypdf adds 395480 bytes. Actual baseline capability ZIP: 22157695 bytes; uncompressed package: 35154955 bytes; unpacked PDF runtime adds 19004181 bytes. Local full fresh-process wait: 2468.294ms; relocated fresh-process wait: 2668.311ms. See JSON for exact values and boundaries. The local processing sample is a few seconds, not a demonstrated excessive tens-of-seconds wait, but there is no agreed SLA and it says nothing about real research duration.

PDF content contains actual run paths/retrieval times; different runs/relocations need not have the same PDF hash or byte count. Each individual run verifies full readback, unchanged Evidence, consistent MD/HTML and embedded fonts. Current verified environment: Windows x64 / CPython 3.12.4. Ubuntu, macOS and Python3.11 not executed because the Issue #13 Pillow-required stop condition applies. No workflow was added. Not a final Skill release, platform support declaration or independent review.
